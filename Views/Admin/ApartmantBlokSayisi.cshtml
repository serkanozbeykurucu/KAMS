@model KAMS.Models.ApartmantAidatVDetail


@{
    var sonuc = 0;
}
@for (int i = 0; i <= Model.Blocs.Count(); i++)
{
    sonuc = i;
}

@Html.DisplayFor(modelItem => sonuc)

@*@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BLOCNAME)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ApartmantDetails.NAME)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
                @Html.ActionLink("Details", "Details", new { id=item.ID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ID })
            </td>
        </tr>
    }*@

